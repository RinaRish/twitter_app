%h3 Users
.span8
  %table.table.table-condensed
    %thead
      %tr
        %th Username
        %th Email
        %th Registered
        %th Role
        %th
        %th
    %tbody
      - @users.each do |user|
        %tr
          %td= link_to user.name, user
          %td= user.email
          %td= user.created_at.to_date
          %td= user.roles.first.name.titleize unless user.roles.first.nil?
          %td
            #follow_form
              - if current_user.following?(user)
                = link_to "Unfollow", {:partial => 'unfollow'}, {:class => "btn"}
              - else
                = link_to "Follow", {:partial => 'follow'}, {:class => "btn"}


